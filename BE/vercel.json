{
  "buildCommand": "npx prisma generate && npx prisma db push && npm run build && npm prune --production",
  "functions": {
    "api/index.ts": {
      "includeFiles": ["dist/**", "node_modules/**"]
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}